# English Hub — CHANGELOG v10.1

## تحسين تقرير نتائج الطالب PDF

تم تحديث تقرير نتائج الطالب مع الحفاظ على الألوان والهوية البصرية الحالية، وإضافة بيانات ديناميكية من الحسابات وقاعدة البيانات.

### الهوية والمحتوى

- تصحيح اسم المنصة إلى `ENGLISH HUB` مع وجود فراغ واضح بين الكلمتين.
- إضافة اسم الأستاذ في رأس التقرير بصيغة واضحة.
- إضافة العبارة التسويقية المختصرة: `متابعة • التزام • مراقبة • تأهيل • تطوير`.
- إضافة رسالة ختامية للأهل توضّح أن المنصة تتابع الطالب وتراقب تقدمه وتساعده على التطور والنجاح.
- تصحيح صياغة تاريخ الإصدار إلى: `تم إصدار التقرير بتاريخ: [التاريخ]`، ويُنشأ التاريخ تلقائياً لحظة توليد التقرير.
- الإبقاء على اتجاه التقرير RTL ودعم عرض اللغة العربية عبر مسار HTML ثم html2canvas وjsPDF.

### البيانات الديناميكية

عند توليد التقرير، يجري تحميل أو تحديد:

- اسم ورقم هاتف المعلم من الحساب الحالي إذا كان المنشئ معلماً.
- المعلم المرتبط بمجموعة الطالب عند إنشاء التقرير من حساب المدير، عند توفره.
- رقم هاتف الإدارة من حساب إداري.
- اسم ورقم هاتف منشئ التقرير.
- بيانات الطالب ونتائجه ومرحلته ومجموعته من اللقطة الحالية للتقرير.

### حفظ منشئ التقرير

يُحفظ سجل إنشاء التقرير في مجموعة `activity_logs` ويتضمن الطالب، منشئ التقرير، اسم المنشئ ورقمه، اسم المعلم ورقمه، رقم الإدارة، وتاريخ الإصدار. إذا كانت قواعد Firestore القديمة ما زالت منشورة، لا يمنع فشل التسجيل تنزيل ملف PDF؛ لكن يُنصح بنشر ملف `firestore.rules` المرفق/المحدّث قبل الاعتماد النهائي.

### التحقق

- تم تشغيل `npm run build` بنجاح.
- نجح TypeScript وفحص الصلاحية وبناء صفحات Next.js.
- لم تظهر أخطاء ترجمة أو JSX أو استيرادات.
- حُوفظ على Firebase Free Tier: لا توجد خدمة خلفية جديدة أو عملية مجدولة؛ الحفظ يتم بسجل Firestore عند إصدار التقرير فقط.

## إعدادات التشغيل

يجب إعداد متغيرات Firebase العامة ومتغير حساب الخدمة في منصة الاستضافة كما هي موضحة في `.env.example`. لا تضع ملف JSON أو المفتاح الخاص داخل مجلد `public` أو داخل الشيفرة المرسلة إلى المتصفح.

## ملاحظة نشر مهمة

بعد رفع النسخة، يجب نشر قواعد `firestore.rules` من Firebase Console أو باستخدام Firebase CLI حتى يستمر حفظ سجل إنشاء التقرير وفق الصلاحيات الصحيحة، إلى جانب القواعد الأمنية السابقة الخاصة بالأسئلة والطلاب والواجبات.

# English Hub — CHANGELOG v10.3

## إعادة بناء تقرير الطالب PDF

تمت إعادة بناء قالب التقرير بالكامل ليطابق التصميم المرجعي المطلوب، مع الحفاظ على اتجاه الكتابة من اليمين إلى اليسار والهوية الزيتية للمنصة. أصبح القالب بمقاس **A4 Portrait** تقريباً، بإطار زيتي خفيف، بطاقات ذات حدود ناعمة، رأس واضح، وتذييل بسيط.

يظهر في الرأس اسم المنصة `ENGLISH HUB`، واسم المدرس الوحيد في المنصة `الأستاذ مهند علاوي`، وعنوان `تقرير تقدم الطالب`، والوصف `متابعة تعليمية دقيقة ونتائج واضحة`، بالإضافة إلى النسبة الإجمالية الديناميكية.

## البيانات الديناميكية

يعرض التقرير من ملف الطالب الحالي اسم الطالب والمجموعة والصف فقط. وتُحسب نسبة التقدم من عدد الدروس المكتملة مقارنةً بإجمالي الدروس المنشورة في صف الطالب. لا تُعرض نسبة 100% إلا إذا كانت القيمة المحسوبة فعلاً تساوي 100%.

تمت إضافة إجماليات الاختبارات إلى لقطة التقرير: مجموع الدرجات، الدرجة الكلية، النسبة المئوية، ومستوى الأداء. جميع هذه القيم تُحسب من محاولات الطالب المصححة في قاعدة البيانات، ولا توجد قيم ثابتة لاسم الطالب أو المجموعة أو الصف أو الدروس أو النتائج.

## عناصر التصميم

يحتوي قسم تقدم الدروس على تشارت دائري ديناميكي، وعدد الدروس بصيغة `المكتمل من الإجمالي`، ونسبة مئوية، وشريط تقدم أفقي. وتظهر نتائج الاختبارات على شكل نقاط واضحة. وتظهر ملاحظات المدرس على شكل نقاط أيضاً، ويُخفى قسم الملاحظات عند تركه فارغاً.

تم وضع علامة مائية واحدة خافتة ومائلة باسم `الأستاذ مهند علاوي` في منطقة جانبية بعيدة عن النصوص الأساسية، وبشفافية منخفضة حتى لا تؤثر على القراءة أو الرسوم.

## التواصل

يظهر قسم `للتواصل مع الأستاذ` مع اسم الأستاذ ورقم التواصل الذي يدخله المدرس في نافذة الملاحظات قبل إصدار التقرير. يظهر بجانب الرقم رمز واتساب أخضر ورابط مباشر قابل للنقر في النسخة الرقمية.

## تقليل حجم الملف

تم تحسين `html2canvas` باستخدام دقة مناسبة للقالب، وتحويل الصورة الداخلية من PNG إلى JPEG مضغوط بجودة 78%، مع تفعيل ضغط jsPDF. هذا يقلل حجم الملف بشكل كبير مقارنةً بالتصدير السابق الذي كان يستخدم PNG و`scale: 2`، مع الحفاظ على وضوح النصوص والعناصر عند الطباعة والمشاركة.

## التحقق

تم تشغيل `npm run build` بنجاح، ونجح فحص TypeScript وJSX وتجميع صفحات Next.js. لم يتم تغيير مخطط قاعدة البيانات أو وظائف الطلاب والدروس والاختبارات أو قواعد Firestore في هذا التحديث؛ التعديل محصور في لقطة التقرير وقالب PDF ومحرك التصدير.

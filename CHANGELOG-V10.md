# English Hub v10

## المسار التعليمي

تم تغيير شكل صفحة دروس الطالب بحيث أصبحت الوحدة هي المستوى الرئيسي الواضح، بينما تظهر الدروس تحتها كبطاقات أصغر ومزاحة للداخل. أضيف خط رأسي متدرج وخط أفقي قصير يصل كل درس بالوحدة، مع رقم ترتيب يوضح: درس 1 من الوحدة، درس 2 من الوحدة، وهكذا.

بطاقة الوحدة أصبحت أعلى بصرياً من بطاقة الدرس، وتحتوي على شريط علوي مميز، اسم الوحدة، عدد الدروس، المكتمل، المتبقي، ونسبة الإنجاز. أما الدرس فأصبح بطاقة مسار مختصرة مع نقطة اتصال وحالة مكتمل/متبقي.

## آلية الإشعارات الحالية والمحدثة

### الطالب

يستقبل الطالب إشعارات داخل التطبيق عند نشر درس أو ملف أو فيديو أو واجب أو اختبار أو إعلان. كما أصبح يستقبل إشعاراً عند اكتمال تصحيح واجبه من الأستاذ.

عند وصول إشعار جديد والتطبيق مفتوح، يظهر تنبيه منبثق أعلى الشاشة لمدة قصيرة. النقر عليه يفتح الصفحة المرتبطة ويحذف الإشعار من القائمة كما كان سابقاً. ويمكن فتح الجرس لرؤية آخر 20 إشعاراً أو مسحها.

### المدرس والمدير

يستقبل المدرس إشعاراً داخل التطبيق عند تسليم طالب واجباً. عند وصوله والتطبيق مفتوح يظهر المنبثق فوراً، والنقر عليه يفتح صفحة تصحيح الواجب.

### الصوت

أصبح هناك صوت تنبيه خفيف يتم توليده داخل المتصفح بدون ملف صوت خارجي أو خدمة مدفوعة. بسبب سياسة المتصفحات، قد يمنع المتصفح الصوت قبل أول تفاعل من المستخدم؛ بعد التفاعل يعمل الصوت عند وصول الإشعار. يمكن إيقافه أو تشغيله من داخل قائمة الجرس، ويحفظ الاختيار محلياً على الجهاز.

هذه ليست إشعارات Push عند إغلاق الموقع. الإشعارات الحالية هي **داخل التطبيق وفي الوقت الفعلي أثناء فتح الموقع**. إشعارات الهاتف الحقيقية عند إغلاق الموقع تحتاج Web Push/Firebase Cloud Messaging وإذن المستخدم، وهي إضافة منفصلة وليست مفعلة في v10.

## Firebase وقواعد الأمان

لم تتغير قواعد Firestore المطلوبة لهذه الإضافة. يعتمد النظام على مجموعة `notifications` الموجودة مسبقاً، وقواعدها تسمح للمستخدم بقراءة إشعاراته فقط. يجب أن تكون قواعد v8/v9 المنشورة في Firebase.

## الفحص

نجح:

```bash
npx tsc --noEmit
npm run build
```

وتأكد أن صفحات دروس الطالب، جرس الإشعارات، صفحة تصحيح الواجب، ومسارات API الأساسية تُبنى دون أخطاء.

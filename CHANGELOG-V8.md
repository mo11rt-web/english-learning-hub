# English Hub v8

## ما تم تنفيذه

### واجهة الطالب

تم إزالة بطاقة بدء الكويز المكررة من صفحة الدرس، وأصبح هناك زر واحد واضح بعد محتوى الدرس: ينتقل إلى الكويز إذا وُجدت أسئلة، أو ينهي الدرس إذا لم يوجد كويز. أزيل الزر الثابت الذي كان يتداخل مع شريط التحكم السفلي، وأصبحت مساحة المحتوى النهائية آمنة على الهاتف.

### بنك الأسئلة للمعلم

تمت إعادة بناء صفحة بنك الأسئلة مع شرح صغير يتغير حسب نوع السؤال. الأنواع المدعومة الآن هي:

* اختيار من متعدد: خيارات واضحة وتحديد الإجابة الصحيحة من قائمة.
* صح أو خطأ.
* إكمال الفراغ: أربعة خيارات إلزامية مع تحديد الكلمة الصحيحة.
* ترتيب: عبارات في أسطر، ويعيد الطالب ترتيبها.
* مطابقة: أزواج بالشكل `left => right`.
* إجابة قصيرة: يمكن تحديد إجابات مقبولة للتصحيح التلقائي أو تركها فارغة للمراجعة اليدوية.
* سؤال مقالي: إجابة مفتوحة بلا مفتاح إجابة، يراجعها الأستاذ.

البيانات القديمة التي تستخدم `options` و`correctAnswer` تبقى قابلة للقراءة.

### واجهة الطالب للواجبات

أصبحت واجهة الحل تعرض مكونات مناسبة لكل نوع: بطاقات اختيار، قائمة خيارات للفراغ، أزرار أعلى/أسفل للترتيب، قوائم للمطابقة، ومساحة كتابة للإجابة القصيرة والمقالية مع تنبيه بأنها تحتاج مراجعة الأستاذ.

### التصحيح والمراجعة

ينشئ الخادم نتيجة لكل سؤال. الأسئلة الموضوعية تُصحح تلقائياً، بينما تتحول المحاولة إلى `pending-review` عند وجود سؤال يدوي. يستطيع الأستاذ إعطاء درجة وتعليق لكل سؤال، ثم تصبح المحاولة `graded` بعد مراجعة جميع الأسئلة اليدوية. لا تُمنح نقاط الإنجاز قبل اكتمال المراجعة. تقرير الطالب يعرض المحاولة المعلقة بعبارة "بانتظار مراجعة الأستاذ" ولا يدخلها في متوسط الدرجات.

### إدارة الواجبات

أضيفت في صفحة الواجبات أزرار:

* **تعديل:** تعديل العنوان، النوع، المجموعة المستهدفة، والأسئلة المختارة.
* **حذف:** حذف مستند الواجب فقط بعد تأكيد المعلم؛ أسئلة بنك الأسئلة لا تُحذف لأنها قد تكون مشتركة مع واجبات أخرى.

## الفحص

* `npx tsc --noEmit`: ناجح.
* `npm run build`: ناجح.
* تم الحفاظ على مسارات API الحالية وقواعد الأمان؛ أسئلة بنك الأسئلة لا تُرسل للطالب مع مفاتيح الإجابات.

## ملاحظة قبل النشر

بعد رفع v8 إلى GitHub وتنفيذ Deploy في Vercel، اختبر عملياً بحساب معلم وحساب طالب: أنشئ سؤالاً من كل نوع، أنشئ واجباً، حلّه، ثم راجع الأسئلة اليدوية من صفحة التصحيح. لا تحذف أسئلة بنك الأسئلة عند حذف واجب؛ الحذف يزيل الواجب فقط كما هو موضح أعلاه.

# English Hub — CHANGELOG v10.5

## الإصدار النهائي لتقرير الطالب PDF

تمت مراجعة ملف PDF المرسل ومقارنته بصرياً مع الصورة المرجعية. كان سبب الاختلاف الأساسي أن القالب كان يرسم التقرير بخط احتياطي مع تباعد غير ثابت، بينما كان التصميم المرجعي يعتمد على خط عربي واضح ومسافات محسوبة، كما أن رسم الدائرة لم يكن مستقراً في الإخراج السابق.

### الخط العربي والتباعد

تم ربط قالب التقرير بمتغير خط Cairo الحقيقي الذي تحمله Next.js (`var(--font-arabic)`) بدلاً من الاعتماد على اسم خط قد لا يكون محملاً داخل صفحة التصوير. كما ينتظر محرك التصدير اكتمال `document.fonts.ready` وإطار رسم إضافياً قبل بدء الالتقاط. هذا يمنع التقاط النص بخط بديل يسبب شكل الترجمة الحرفية أو المسافات غير الطبيعية.

تم ضبط اتجاه RTL، وترك التباعد الطبيعي للكلمات العربية دون `letter-spacing` مصطنع، مع تثبيت أوزان العناوين والنصوص والمسافات بين الأقسام وفق التصميم المرجعي.

### الرسم والجودة

تم استخدام SVG فعلي لرسم حلقة التقدم، مع مسار خلفي زيتي فاتح ومسار أمامي زيتي ديناميكي يعتمد على النسبة المحسوبة من النظام. أضيفت النسبة وعدد الدروس داخل الحلقة، مع شريط تقدم أفقي مستقل.

تمت إعادة التصدير إلى PNG عالي الجودة لأن PNG يحافظ على حواف الحروف العربية والألوان المسطحة وأشكال SVG أفضل من JPEG في هذا النوع من التقارير. ما زال ضغط jsPDF مفعلاً لتقليل الحجم دون التضحية بجودة الخط والتصميم.

### الصياغة والتصميم

تم اعتماد صياغة عربية طبيعية ومباشرة، مثل `متابعة تعليمية دقيقة، نتائج واضحة، وتقدم مستمر` و`لقد أتممت جميع الدروس بنجاح`. تظهر نتائج الاختبارات والملاحظات كنقاط منظمة، ويُخفى قسم ملاحظات المعلم عند عدم وجود ملاحظات بدلاً من عرض نص وهمي.

يتضمن التقرير رأساً مطابقاً للصورة المرجعية، ومعلومات الطالب الثلاث فقط: الاسم والمجموعة والصف، وقسم التقدم، ونتائج الاختبارات، والتواصل مع الأستاذ مهند علاوي، والعلامة المائية الخافتة، والتذييل التعليمي.

### الاختبار

تم تشغيل `npm run build` بنجاح بعد التغييرات، ونجح فحص TypeScript وJSX وتجميع صفحات Next.js. تمت مراجعة ملف PDF المرسل بصرياً وتسجيل نقاط الاختلاف قبل تطبيق التصحيحات. الحزمة النهائية لا تتضمن أسراراً أو مجلدات `node_modules` و`.next`.

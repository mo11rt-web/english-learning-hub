# English Hub v9

## 1. تقدم الوحدات للطالب

تم تحديث صفحة **دروسي** لتجميع الدروس حسب الوحدة المنشورة. لكل وحدة تظهر:

* اسم الوحدة.
* إجمالي عدد الدروس.
* عدد الدروس المكتملة.
* عدد الدروس المتبقية.
* نسبة التقدم المئوية.
* شريط تقدم متناسق مع ألوان الثيم.
* إبراز الوحدة المكتملة عند الوصول إلى 100%.

تتم مطابقة الدروس مع مجموعات الطالب قبل حساب النسبة، لذلك لا تدخل دروس المجموعات الأخرى في التقدم. وإذا وُجد درس قديم بلا وحدة، يظهر ضمن قسم **دروس أخرى** بدلاً من اختفائه.

تم تجنب إضافة فهرس Firestore مركب جديد؛ تُجلب الوحدات المنشورة حسب المرحلة والحالة ثم تُرتب محلياً.

## 2. تقرير PDF للطالب

من قائمة إجراءات الطالب في صفحة إدارة الطلاب، عند اختيار **تصدير تقرير PDF** تظهر نافذة قبل الإصدار تطلب **ملاحظات المعلم العامة**. بعد الضغط على إنشاء التقرير يتم تجهيز تقرير بصري يتضمن:

* اسم الطالب والمرحلة والمجموعة.
* مؤشر دائري لنسبة إنجاز الدروس.
* بطاقات النقاط والمستوى ومتوسط الاختبارات وعدد الدروس.
* شريط تقدم الدروس.
* مؤشر الترتيب داخل المجموعة عند توفره.
* مخطط أشرطة لآخر نتائج الواجبات والاختبارات.
* تمييز المحاولات التي ما زالت بانتظار المراجعة اليدوية.
* ملاحظات المعلم داخل التقرير.
* تاريخ إصدار التقرير.

## 3. المشاركة عبر الهاتف وواتساب

بعد إنشاء التقرير تظهر نافذة **تقرير الطالب جاهز**. زر **مشاركة ملف PDF** يستخدم نافذة المشاركة الأصلية في الهاتف؛ يمكن اختيار واتساب وإرفاق الملف مباشرة من النظام. على الكمبيوتر يتم تنزيل الملف. زر **فتح واتساب** يفتح محادثة برسالة جاهزة، ويُرفق المعلم ملف PDF الذي تم تنزيله يدوياً.

إذا كان رقم ولي الأمر محفوظاً، يُستخدم في رابط واتساب. وإذا لم يكن محفوظاً، يُفتح واتساب دون رقم محدد.

## 4. قواعد Firebase

لا توجد قاعدة Firestore جديدة مطلوبة لهذه الإضافة؛ قواعد الوحدات الحالية تسمح للطالب النشط بقراءة الوحدات، وقواعد الدروس الحالية تسمح بقراءة الدروس المنشورة. يجب فقط التأكد من نشر `firestore.rules` الموجود في النسخة الحالية إذا كانت بيئة Firebase لا تزال على نسخة أقدم.

## 5. الفحص

تم تشغيل:

```bash
npx tsc --noEmit
npm run build
```

ونجح الفحص والبناء بعد إصلاح حقل المستوى في قالب PDF.

## 6. اختبار النشر المقترح

بعد رفع النسخة إلى Vercel، سجّل الدخول بحساب طالب لديه وحدات ودروس منشورة وتقدم محفوظ. تحقق من ظهور أسماء الوحدات وعداداتها وشريط التقدم. ثم سجّل الدخول بحساب المعلم، اختر طالباً، اضغط تصدير تقرير PDF، اكتب ملاحظات، وأنشئ التقرير، ثم جرّب المشاركة من الهاتف وفتح واتساب.

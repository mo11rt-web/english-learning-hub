# English Hub — CHANGELOG v10.2

## إعادة تصميم تقرير تقدم الطالب

أُعيد بناء قالب التقرير ليكون بسيطاً وواضحاً ومناسباً للطباعة والمشاركة مع ولي الأمر، مع الحفاظ على هوية English Hub واستخدام الألوان الزيتية والأسود والأحمر بصورة متناسقة.

أصبح ملف PDF يستخدم قياس **Letter** الحقيقي، مع ضبط أبعاد القالب الداخلي على النسبة نفسها وملاءمته داخل صفحة PDF دون تشويه أو قص. كما أضيفت علامة مائية مائلة وخفيفة باسم **الأستاذ مهند علاوي** في الخلفية، بحيث لا تؤثر على قراءة النصوص.

يعرض التقرير من بيانات الطالب القادمة من النظام المعلومات المطلوبة فقط: **اسم الطالب، المجموعة، والصف**. أما بيانات التقدم فتظهر في تشارت دائري لنسبة الإنجاز، وشريط تقدم أفقي، ونتائج الاختبارات المختصرة.

## نافذة ملاحظات المدرس

قبل إنشاء التقرير، تطلب النافذة من المدرس إدخال رقم التواصل الخاص بالأستاذ مهند علاوي، إضافة إلى الملاحظات العامة. الرقم إلزامي حتى لا يصدر التقرير ببيانات اتصال ناقصة، ويتم تنظيف الفراغات والرموز غير الضرورية قبل حفظه وعرضه.

يظهر رقم التواصل داخل التقرير إلى جانب علامة واتساب خضراء ورابط مباشر يفتح محادثة واتساب عند توفر الرقم بصيغة دولية مناسبة.

## اللغة والتنسيق

تمت مراجعة النصوص الجديدة وصياغتها بالعربية الواضحة، مع إزالة عبارات الإدارة والمنشئ غير المطلوبة من النسخة السابقة، وتوحيد المسافات واتجاه RTL. جرى تحويل ملاحظات المدرس إلى قائمة نقطية تلقائياً؛ فإذا كتب المدرس عدة جمل أو أسطراً، تُعرض كل فكرة في نقطة مستقلة. كما تظهر نتائج الاختبارات ضمن قائمة نقطية مع شريط نسبة لكل نتيجة.

## التحقق

تم تشغيل `npm run build` بنجاح، ونجح فحص TypeScript وJSX وتجميع صفحات Next.js. لا توجد خدمة مدفوعة جديدة، ولا تغيير في بنية Firebase الأساسية؛ التعديل متوافق مع Firebase Free Tier.

## النشر

يجب رفع الحزمة الجديدة، ثم التأكد من نشر `firestore.rules` السابقة والمحدّثة من Firebase Console أو Firebase CLI. كما يجب التأكد من ضبط متغيرات البيئة الموجودة في `.env.example`، خصوصاً `FIREBASE_SERVICE_ACCOUNT_JSON` عند استخدام وظائف Firebase Admin.

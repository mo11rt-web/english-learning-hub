# تقرير تسليم English Hub — v6

تم تنفيذ الإصلاحات مباشرة على النسخة المحدثة `english-hub-fixed-v5` دون إعادة هيكلة بقية النظام. النسخة الناتجة تعالج مشاكل الطلاب، الدروس، الاختبارات، الواجبات، وتغيير كلمات المرور ضمن الملفات الضرورية فقط.

## الإصلاحات المنفذة

| المشكلة | الإصلاح المنفذ |
|---|---|
| إعادة إضافة طالب بنفس رقم الهاتف بعد الحذف | الحذف الأول أصبح نقلًا إلى سلة المهملات عبر `status: deleted` مع استرجاع. أضيف زر «حذف نهائي» للمحذوفين، ويستدعي API خادميًا يحذف حساب Firebase Authentication وملف `profiles` والبيانات التابعة في `attempts` و`lesson_progress` و`notifications` و`shares`. بعد الحذف النهائي يمكن استخدام رقم الهاتف من جديد. |
| تغيير كلمة المرور لا يعمل | تم تحسين API تغيير كلمة المرور ليجدد التحقق من الصلاحيات، يتأكد أن الهدف طالب، ويتعامل مع أخطاء الجلسة والحساب وإعدادات Firebase Admin برسائل دقيقة. واجهة المعلم تطلب رمز دخول محدثًا قبل الإرسال. |
| الدروس لا تظهر أو تظهر رسالة عدم الصلاحية | تم تبسيط استعلام الطالب إلى المرحلة + حالة النشر، مع تصفية المجموعات في الواجهة. هذا يدعم الدروس القديمة ذات `targetGroupIds` الفارغة والدروس الجديدة التي تستخدم `__all__`. تم تحديث قواعد Firestore لتوافق الاستعلام. |
| الواجبات لا تظهر عند استخدام `__all__` | تم توحيد تصفية الواجبات مع `matchesStudentGroups` لدعم المجموعات القديمة والجديدة. |
| الإجابة الصحيحة للأفعال الشاذة تظهر للطالب | لم تعد الخيارات غير المختارة تُلوّن باعتبارها صحيحة، ولم تعد رسالة الخطأ تعرض نص الإجابة الصحيحة. يظهر التلوين للخيار الذي اختاره الطالب فقط. |
| بنك أسئلة الواجب يرسل `correctAnswer` إلى المتصفح | أضيف مسار API لجلب نسخة عامة من الأسئلة دون `correctAnswer` أو `explanation`، ومسار خادمي للتصحيح وحفظ المحاولة ومنح النقاط. تم منع الطالب من قراءة `question_bank` مباشرة في قواعد Firestore. |
| عدم إضافة أكثر من واجب | يتم تفريغ نموذج الواجب فور نجاح إنشاء المستند وقبل إرسال الإشعارات، مع حالة حفظ تمنع الضغط المتكرر ورسالة نجاح/فشل. فشل الإشعار لا يعيد إنشاء الواجب ولا يمنع إضافة واجب آخر. |
| ظهور خطأ صلاحية بعد تسليم الواجب | نقل إشعار المعلم إلى مسار التصحيح الخادمي بدل استعلام الطالب عن ملفات المعلمين المحمية. |

## المسارات الجديدة

| المسار | الوظيفة |
|---|---|
| `/api/admin/delete-student` | التحقق من المعلم/المدير والحذف النهائي من Auth وFirestore والبيانات التابعة. |
| `/api/student/assignment-questions` | إعادة أسئلة الواجب العامة دون مفاتيح التصحيح. |
| `/api/student/grade-assignment` | التحقق من الواجب والمجموعات وعدد المحاولات، التصحيح الخادمي، حفظ المحاولة، منح النقاط، وإشعار المعلمين. |

## الفحص المنفذ

تم تشغيل `npm run build` بنجاح، ونجح `npx tsc --noEmit` دون أخطاء TypeScript. أداة `npm run lint` في المشروع غير مهيأة وتطلب إنشاء إعداد ESLint تفاعليًا، لذلك لم يتم إنشاء ملف إعداد جديد حتى لا يتغير نطاق المشروع.

الفحص المحلي يؤكد سلامة التجميع، لكنه لا يستطيع اختبار حسابات Firebase الحقيقية دون بيانات المشروع وبيئة Firebase Admin. يجب بعد النشر تجربة السيناريوهات التشغيلية بحساب معلم وحساب طالب فعليين.

## إعدادات النشر المطلوبة

يجب تعريف متغيرات Firebase Admin على الخادم فقط، وليس في المتصفح:

```env
FIREBASE_ADMIN_PROJECT_ID=...
FIREBASE_ADMIN_CLIENT_EMAIL=...
FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\\n...\\n-----END PRIVATE KEY-----\\n"
```

يمكن أن يعتمد `FIREBASE_ADMIN_PROJECT_ID` على `NEXT_PUBLIC_FIREBASE_PROJECT_ID` كبديل، لكن يجب توفير البريد والمفتاح الخاصين بحساب الخدمة حتى تعمل عمليات تغيير كلمة المرور والحذف النهائي والتصحيح الخادمي.

بعد نشر النسخة يجب نشر قواعد Firestore الجديدة أيضًا، لأن منع قراءة `question_bank` من الطالب وتغيير قراءة الدروس لن يُطبق على المشروع السحابي قبل نشر ملف `firestore.rules`.

## اختبار قبول سريع بعد النشر

ينبغي إنشاء طالب تجريبي، نقله إلى سلة المهملات ثم استرجاعه، ثم حذفه نهائيًا والتأكد من إمكانية إنشاء طالب جديد بنفس الرقم. بعد ذلك يجب تغيير كلمة مروره وتجربة الدخول بالكلمة الجديدة. كما ينبغي نشر درس عام ودرس مخصص لمجموعة، ثم التأكد من ظهورهما للطالب المناسب فقط، وإنشاء واجبين متتاليين، وحل أحدهما، والتأكد من عدم ظهور `correctAnswer` في استجابة المتصفح أو بيانات `question_bank` المقروءة مباشرة.

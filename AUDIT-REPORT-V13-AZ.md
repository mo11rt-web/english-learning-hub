# تقرير فحص English Hub — A to Z

## نطاق الفحص

تم فحص نسخة English Hub الحالية بعد v13 على مستوى البناء، TypeScript، المسارات العامة والمحمية، حماية API، منطق الأدوار، قواعد Firestore، استقلال المشروع عن Firebase Storage، وتجربة صفحة الدخول وقت التشغيل. كما تمت مراجعة تدفقات المدرس والطالب من المصدر لجميع الوحدات الرئيسية.

## الاختبارات المنفذة فعلياً

| الاختبار | النتيجة |
|---|---|
| `npm run build` | ناجح دون أخطاء TypeScript أو JSX |
| `npx tsc --noEmit` | ناجح |
| صفحات التطبيق العامة والمحمية | مرّت smoke test دون 500 أو 404 غير متوقعة |
| التحويل إلى Login عند فتح Dashboard دون جلسة | ناجح |
| صفحة Login بدون متغيرات Firebase | أصبحت تعمل وتعرض رسالة إعداد واضحة بدلاً من Application Error |
| API أسئلة الواجب دون رمز | 401 مع رسالة تسجيل دخول واضحة |
| API تصحيح الواجب دون رمز | 401 مع رسالة تسجيل دخول واضحة |
| API حذف الطالب دون رمز | 401 مع رسالة تسجيل دخول واضحة |
| API ترتيب الطالب دون رمز | 401 مع رسالة تسجيل دخول واضحة |
| استقلال الكود عن Firebase Storage | لا توجد عمليات رفع Firebase Storage؛ أزيل آخر import غير مستخدم |
| `npm run lint` | لم يُنفذ فعلياً لأن Next.js طلب إنشاء إعداد ESLint تفاعلياً، ولم يتم تغيير إعدادات المشروع أثناء الفحص |

## فحص المدرس

### المصادقة والصلاحيات

يوجد حارس مركزي في `AppShell` يتحقق من وجود الحساب، حالة الحساب، ودور المستخدم. حساب الطالب يُعاد إلى واجهة الطالب، والحساب الإداري/التعليمي يُعاد إلى Dashboard. كما أن اختيار القسم أو مساحة العمل مطلوب للمعلم قبل صفحات الإدارة.

### إدارة الطلاب

تمت مراجعة مسارات الإضافة والتعديل والحذف والاسترجاع والحذف النهائي والنسخ الاحتياطي والاستعادة وتغيير كلمة المرور عبر صفحات الطلاب وواجهات API الإدارية. عمليات الحذف الحساسة لا تعتمد على الواجهة فقط، بل تمر عبر API مع التحقق من رمز Firebase وصلاحية المدير.

### المجموعات والوحدات والدروس

تمت مراجعة إنشاء المجموعات والوحدات والدروس، النشر وإلغاء النشر، استهداف المجموعات، حذف الدرس، وحذف الوحدة مع الدروس المرتبطة. باني المحتوى المشترك مستخدم في الإضافة والتعديل، والبحث والفلترة والترتيب في دروس الوحدة تحفظ حالتها عبر `sessionStorage`.

### المحتوى التعليمي

تمت مراجعة الكلمات، الأفعال الشاذة، الصوت، الفلاتر، والاستيراد والإدارة. عرض الأفعال أصبح جدولاً يحافظ على زر النطق لكل تصريف، مع بقاء التعديل والحذف والتفعيل والتعطيل.

### بنك الأسئلة والواجبات

تمت مراجعة أنواع الأسئلة المدعومة، التحقق من المدخلات، الحقول الاختيارية، تخزين الإجابات، التعديل والحذف والنشر. الإجابات الصحيحة لا تُرسل إلى الطالب؛ تحميل أسئلة الواجب والتصحيح يمران عبر API الخادم. مسارات التصحيح تتعامل مع الأسئلة الآلية والأسئلة التي تحتاج مراجعة يدوية.

### التقارير والإشعارات

تمت مراجعة منطق تقرير PDF، معلومات الطالب، نتائج الاختبارات، RTL، التاريخ العربي، وحجم التصدير. تمت مراجعة نظام الإشعارات اللحظية والجرس والروابط الداخلية، بما فيها إشعارات تسليم الواجب والرد على الاستفسارات.

## فحص الطالب

تمت مراجعة تسجيل الدخول، الحماية حسب الدور، الصفحة الرئيسية، الإعلانات، لوحة المتفوقين، الدروس والوحدات، كتل المحتوى، الأسئلة داخل الدرس، الواجبات، المحاولات، التصحيح، النتائج، الأفعال الشاذة، الكلمات، أسئلة الطالب، والإشعارات.

قواعد Firestore تمنع الطالب من قراءة `question_bank` مباشرة، ومسار API يعيد نسخة عامة من السؤال من دون `correctAnswer` أو بيانات التصحيح الحساسة. كما أن محاولة الطالب مربوطة بـ `studentId`، وتتحقق API من أن الواجب منشور وموجه إلى مجموعته.

## الأمن وقواعد Firebase

تمت مراجعة `firestore.rules` للمجموعات التالية: profiles، stages، groups، units، lessons، question_bank، announcements، public_leaderboards، assignments، attempts، lesson_progress، inquiries/messages، notifications، activity_logs، وshares.

تم التأكد من أن صور الإعلانات والصور التعليمية لا تحتاج Firebase Storage داخل الكود الحالي؛ صور Cloudinary تستخدم رابطاً محفوظاً في Firestore، بينما PDF والصوت والملفات الأخرى تستخدم روابط خارجية. لذلك لا تحتاج النسخة الحالية إلى تفعيل Firebase Storage.

## ملاحظات يجب الانتباه إليها

| الأولوية | الملاحظة | الأثر |
|---|---|---|
| عالية قبل النشر | يجب ضبط كل متغيرات Firebase العامة ومتغير `FIREBASE_SERVICE_ACCOUNT_JSON` على الاستضافة | بدونها لن يعمل تسجيل الدخول أو الخادم |
| عالية عند تعدد المعلمين | قاعدة `inquiries` الحالية تسمح للمعلم/المدير بقراءة الاستفسارات، بينما التصفية حسب القسم تتم في الواجهة. إذا أصبح هناك عدة معلمين مستقلين، يجب تضييق القاعدة حسب `teacherIds` أو `groupIds` | ملاحظة خصوصية مستقبلية |
| متوسطة | Cloudinary Unsigned Preset عام؛ يجب تقييد نوع الملفات وحجمها ومجلد الرفع من إعدادات Cloudinary | منع إساءة استخدام رابط الرفع |
| منخفضة | `npm run lint` يحتاج إعداد ESLint أول مرة | لا يمنع build، لكنه مطلوب لتحسين CI |
| تشغيلية | الاختبار الكامل لتدفقات Firestore الحقيقية، رفع Cloudinary، الإشعارات اللحظية، وPDF يحتاج حساب مدرس وحساب طالب وبيانات Firebase فعلية | لا يمكن إثباته من بيئة بلا أسرار أو جلسة مستخدم |

## النتيجة

البناء والأنواع والمسارات العامة وحماية API تعمل بنجاح. لا توجد مشكلة compile أو import تمنع النشر. تم أيضاً إصلاح انهيار runtime الذي كان يظهر عند غياب إعدادات Firebase، فأصبحت صفحة الدخول تعرض رسالة واضحة بدلاً من Application Error.

النسخة مناسبة للانتقال إلى اختبار قبول فعلي بحساب مدرس وحساب طالب. قبل اعتبارها مختبرة 100% في الإنتاج، يجب تنفيذ سيناريو قبول يدوي بحسابين حقيقيين: إنشاء طالب، إنشاء مجموعة، إنشاء وحدة ودرس، نشر واجب، حل الواجب، إرسال سؤال، الرد عليه، إنشاء إعلان، رفع صورة Cloudinary، وتصدير تقرير PDF.
